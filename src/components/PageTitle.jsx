import React from "react";

const PageTitle = ({ title }) => {
  return (
    <div className=" md:!mb-[5%] my-3 z-10 py-6 text-center text-white w-full font-jost text-2xl font-bold">
      {title}
    </div>
  );
};

export default PageTitle;
